# 과제 목표

## Python 기초

- 변수가 무엇이고, 왜 사용하는지
- int, str, bool, list, dict의 차이
- if/elif/else로 조건에 따라 다른 동작을 수행
- for와 while의 차이를 설명하고 적절히 선택
- 함수를 정의하고, 매개변수와 반환값을 활용

## 클래스와 객체

- 클래스가 무엇이고, 왜 사용하는지
- __init__ 메서드와 self의 역할
- 클래스의 속성(attribute)과 메서드(method)를 정의하고 활용

## 파일 입출력

- 파일을 열고, 읽고, 쓰는 기본 과정
- JSON 형식이 무엇이고, 왜 데이터 저장에 사용하는지
- try/except를 사용하여 오류를 처리

## Git 기초

- Git이 무엇이고 왜 필요한지
- init, add, commit, push, pull, checkout, clone이 각각 무엇을 하는지
- 브랜치를 생성하고 병합
- 원격 저장소를 clone하고, pull로 변경사항을 가져오는 방법
