# 학습 정리 by Gemini

## 1. Python 기초

### 변수가 무엇이고, 왜 사용하는가?

* **개념:** 데이터를 저장하는 메모리 공간의 이름(상자).
* **사용 이유:** 데이터를 지속적으로 저장하고 재사용하여 코드의 가독성을 높이고 관리하기 위해 사용합니다.

### 자료형 차이 (`int`, `str`, `bool`, `list`, `dict`)

* **`int` (정수):** 소수점이 없는 숫자 (`10`, `-3`).
* **`str` (문자열):** 따옴표로 감싸진 텍스트 (`"hello"`).
* **`bool` (불리언):** 참/거짓 값 (`True`, `False`).
* **`list` (리스트):** 순서가 있는 값의 목록, 인덱스로 접근 (`[1, 2, 3]`).
* **`dict` (딕셔너리):** Key-Value(키-값) 쌍으로 이루어진 구조, 키로 접근 (`{"name": "Kim"}`).

### `if`/`elif`/`else` 조건문

* 조건에 따라 코드 실행 흐름을 제어합니다.

```python
if score >= 90:
    print("A")
elif score >= 80:
    print("B")
else:
    print("C")

```

### `for`와 `while`의 차이 및 선택

* **`for`:** 반복 횟수가 정해져 있거나 sequence(리스트, 범위 등)를 순회할 때 사용.
* **`while`:** 특정 조건이 참(`True`)인 동안 계속 반복할 때 사용.

### 함수 정의, 매개변수, 반환값

* **함수:** 특정 작업을 수행하는 코드 묶음 (`def`).
* **매개변수 (Parameter):** 함수 내부로 전달받는 입력값.
* **반환값 (Return value):** 함수 처리 결과를 외부로 전달하는 출력값.

```python
def add(a, b):  # a, b는 매개변수
    return a + b # 반환값

```

---

## 2. 클래스와 객체

### 클래스가 무엇이고, 왜 사용하는가?

* **개념:** 객체를 만들어내기 위한 **설계도(붕어빵 틀)**.
* **사용 이유:** 연관된 데이터(속성)와 기능(메서드)을 하나로 묶어 코드의 재사용성과 관리 효율성을 극대화하기 위해 사용합니다.

### `__init__` 메서드와 `self`

* **`__init__`:** 객체가 생성될 때 자동으로 호출되는 **초기화(생성자) 메서드**.
* **`self`:** 생성된 **객체 인스턴스 자기 자신**을 가리키는 참조 변수.

### 속성(Attribute)과 메서드(Method)

* **속성:** 클래스 내부에 정의된 **변수 (데이터/상태)**.
* **메서드:** 클래스 내부에 정의된 **함수 (동작/기능)**.

```python
class Dog:
    def __init__(self, name):
        self.name = name  # 속성

    def bark(self):       # 메서드
        print(f"{self.name}가 짖습니다.")

```

---

## 3. 파일 입출력

### 파일 열기, 읽기, 쓰기

* `open()` 함수로 파일을 열고, `read()` / `write()` 후 반드시 `close()`를 해주어야 합니다. `with` 구문을 사용하면 파일이 자동으로 닫힙니다.

```python
# 쓰기
with open("test.txt", "w", encoding="utf-8") as f:
    f.write("Hello")

# 읽기
with open("test.txt", "r", encoding="utf-8") as f:
    text = f.read()

```

### JSON 형식이란?

* **개념:** 텍스트 기반의 표준 데이터 교환 형식 (Key-Value 구조).
* **사용 이유:** 사람이 읽기 쉽고, 가볍고, 다른 프로그래밍 언어 간에도 쉽게 데이터를 주고받을 수 있기 때문입니다.

### `try`/`except` 예외 처리

* 프로그램 실행 중 발생할 수 있는 에러(오류)를 포착하여 프로그램이 강제 종료되지 않도록 처리합니다.

```python
try:
    result = 10 / 0
except ZeroDivisionError:
    print("0으로 나눌 수 없습니다.")

```

---

## 4. Git 기초

### Git이란 무엇이고 왜 필요한가?

* **개념:** **분산 버전 관리 시스템 (VCS)**.
* **필요성:** 코드의 변경 이력을 기록 및 추적하고, 과거 버전으로 복구하거나 여러 명과의 협업을 효율적으로 진행하기 위해 필수적입니다.

### 주요 Git 명령어 설명

* **`init`:** 현재 디렉토리를 Git 저장소로 초기화.
* **`add`:** 파일 변경사항을 Staging Area(커밋 준비 영역)에 올려둠.
* **`commit`:** Staging Area의 변경사항을 히스토리에 영구 저장.
* **`push`:** 로컬 저장소의 커밋 내역을 원격 저장소(GitHub 등)로 업로드.
* **`pull`:** 원격 저장소의 최신 변경사항을 가져와 현재 로컬 브랜치에 병합.
* **`checkout` (또는 `switch`/`restore`):** 다른 브랜치로 이동하거나 이전 파일 상태로 복원.
* **`clone`:** 원격 저장소를 내 컴퓨터로 그대로 복사해옴.

### 브랜치 생성 및 병합

```bash
git branch feature      # 'feature'라는 브랜치 생성
git checkout feature    # 해당 브랜치로 이동 (또는 git switch feature)
# 작업 완료 후 메인 브랜치로 이동하여 병합
git checkout main
git merge feature       # 'feature' 브랜치 변경사항을 main에 병합

```

### 원격 저장소 가져오기 (`clone`, `pull`)

* **처음 가져올 때:** `git clone <원격 저장소 URL>`
* **최신 변경사항 updates:** `git pull origin main`