# 제출용 스크린샷

`mission_2.md` 제출물 요구사항에 따라 아래 스크린샷을 이 폴더에 저장하세요.

| 파일명 | 내용 |
|---|---|
| `env.png` | 개발 환경 설정 (VSCode, `python --version`, `git config --list`) |
| `menu.png` | 메뉴 화면 |
| `play.png` | 퀴즈 풀기 (출제 → 정답/오답 → 결과 점수) |
| `add_quiz.png` | 퀴즈 추가 (입력 → 완료 메시지) |
| `score.png` | 점수 확인 (최고 점수) |
| `git_log.png` | `git log --oneline --graph` 결과 |

캡처 팁: 재시작 후 `📂 저장된 데이터를 불러왔습니다` 메시지가 보이면 데이터 유지(영속성)까지 함께 증명할 수 있습니다.
